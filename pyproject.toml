[project]
name = "lfp-build"
version = "0.0.1+gae424b9"
description = "Local build utilities"
requires-python = ">=3.11,<3.14"
dependencies = ["mergedeep", "ruff", "tomlkit", "typer", "uv>=0.9.9"]

[project.scripts]
lfp-build = "lfp_build.cli:main"

# test123
[dependency-groups]
dev = ["pytest"]

[build-system]
requires = ["uv_build>=0.9.6,<0.10.0"]
build-backend = "uv_build"
